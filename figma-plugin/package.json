{
  "name": "voc-ai-assistant-figma-plugin",
  "version": "1.0.0",
  "description": "Figma plugin to generate VOC AI Assistant chatbot wireframes",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
